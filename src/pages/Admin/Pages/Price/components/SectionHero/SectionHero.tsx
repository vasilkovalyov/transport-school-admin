import { Box } from '@mui/material';
import Typography from '@mui/material/Typography';

import { HeroForm } from '../../../Components';

export default function SectionHero() {
  return (
    <Box>
      <Typography variant="h2">Section hero</Typography>
      <HeroForm />
    </Box>
  );
}
